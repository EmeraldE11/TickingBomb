using System;
using Byui.Games.Casting;

namespace Diffuse

{
    public class Wires : Actor
    {
        public Wires ()
        {
            // var random = new Random();

            // List<Constants> Colors = new List<Constants>();
            // Colors.Add(Constants.red);
            // Colors.Add(Constants.yellow);
            // Colors.Add(Constants.green);
            // Colors.Add(Constants.blue);
            // Colors.Add(Constants.purple);
            

            // for (int i = 1; i < 6; i++)
            // {
            //     (Constant file type) color = random.Next(Colors.count);
            //     //run through wire logic
            //     //have wire logic assign bool value
                
            //     CreateWire(i, cut, color);
            // }
        }

        // public void CreateWire(int id, bool cut, COnsTants color)
        // {
        //     Point offset = "Maintain X coordinate, lower Y coordinate by particular value";
        //     Point position = wire.GetPosition().Add(offset);
        //     Actor wire = new Actor();
        //     wire.SetText("===============");
        //     wire.SetColor(GetColor());
        // }
    }

}